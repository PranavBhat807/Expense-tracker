/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '',  // Set to '' to ensure paths are not prefixed
    assetPrefix: '',  // Ensure static files are served from the root
  
};

export default nextConfig;
